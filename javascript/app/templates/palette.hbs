{{#each items}}
 <li name={{fieldName}} display_name="{{displayName}}" type={{classType}} class = "{{item_class}} tooltip">{{displayName}}<span class="tooltiptext tooltip-top">{{{help_text}}}</span></li>
{{/each}}
